[package]
name = "sakka-test"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dev-dependencies]
sakka = { workspace = true, default-features = false, features = [
    "std",
    "derive",
] }

trybuild = "1.0.117"
